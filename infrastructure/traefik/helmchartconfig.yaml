# k3s installs Traefik through its packaged Helm chart; HelmChartConfig
# overlays these values onto that install.
apiVersion: helm.cattle.io/v1
kind: HelmChartConfig
metadata:
  name: traefik
  namespace: kube-system
spec:
  valuesContent: |-
    logs:
      access:
        enabled: true
        format: json
        fields:
          headers:
            defaultMode: keep
            names:
              Authorization: drop
              Cookie: drop
              X-Forwarded-For: keep
              User-Agent: keep
